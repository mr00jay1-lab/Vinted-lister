name: vinted-lister
version: 1.0.0
schema: v1

models:
  - uses: ollama/qwen2.5-coder-7b      # local, free, offline
  - uses: anthropic/claude-sonnet-4-5  # cloud primary
    with:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

mcpServers:
  - uses: anthropic/memory-mcp
